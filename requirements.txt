# ---- Core RAG pipeline (all local, no API keys) ----
sentence-transformers>=3.0      # embeddings (all-MiniLM-L6-v2) + cross-encoder reranker
chromadb>=0.5                   # local persistent vector store
requests>=2.31                  # plain HTTP calls to Ollama (localhost:11434)

# ---- Backend / API ----
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
python-multipart>=0.0.9          # file uploads (document ingest)

# ---- Document ingestion (upload your own files) ----
pypdf>=4.0                       # PDF text extraction
python-docx>=1.1                 # .docx text extraction

# ---- Web-search fallback (agentic RAG, keyless) ----
ddgs>=6.0                        # DuckDuckGo search, no API key
beautifulsoup4>=4.12             # extract main text from fetched pages
lxml>=5.0                        # fast HTML parser for BeautifulSoup

# ---- Frontend (trace explorer, Phase 4) ----
streamlit>=1.33
pandas>=2.2                     # aggregate charts / tables in the UI

# ---- Dev / test ----
pytest>=8.0
httpx>=0.27                     # FastAPI TestClient
